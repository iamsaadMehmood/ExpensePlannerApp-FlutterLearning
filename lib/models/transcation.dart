class Transcation {
  final String id;
  final String title;
  final double amount;
  DateTime date;
  Transcation({
    required this.id,
    required this.title,
    required this.amount,
    required this.date,
  });
}
